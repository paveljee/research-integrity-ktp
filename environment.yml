name: research-integrity-ktp
channels:
  - defaults
  - conda-forge # Often good to include for broader package availability
dependencies:
  - python=3.12 # Or your specific Python version, let's assume 3.12 based on recent numpy/pandas
  - pip=25.1
  - poetry=2.1.3
